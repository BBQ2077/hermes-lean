# Normalize line endings so the tool behaves the same on every OS.
# Shell scripts and Python must keep LF or they break on Linux/macOS.
* text=auto eol=lf
*.sh text eol=lf
*.py text eol=lf
*.bat text eol=crlf
*.json text eol=lf
*.md text eol=lf
